export type SearchModel = {
    img: string
    link: string
    release: string
    title: string
}

export type AnimeModel = {
    anime_type: string
    score: string
    title: string
    total_episodes: string
}